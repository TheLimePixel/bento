import { types::types }

def foo(foo: types::Foo) = println("Foooooooooo")

def printBar(bar: types::Bar) = println(bar.name)

def main() = {
    foo(types::Foo)
    let bar: Bar = types::Bar("Bar")
    printBar(bar)
    bar.name = "Baz"
    printBar(bar)
}