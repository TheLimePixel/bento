import { types::types }

fun foo(foo: types::Foo) {
    println("Foooooooooo")
}

fun printBar(bar: types::Bar) {
    println(bar.name)
}

fun main() {
    foo(types::Foo)
    printBar(types::Bar("Bar"))
}